name: Agent Foundation

on:
  push:
    branches:
      - main
      - upgrade/agent-system-foundation
  pull_request:
    branches:
      - main

jobs:
  agent-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install agent test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pandas pydantic

      - name: Compile agent modules
        run: python -m compileall -q src/agent api/routes/intelligence.py

      - name: Run agent foundation tests
        run: pytest -q tests/test_agent_evidence.py tests/test_agent_orchestrator.py
