# PeopleOS Desktop everyday runtime
#
# This is the default local product. It intentionally excludes heavy predictive
# training packages (XGBoost, LightGBM, Optuna, imbalanced-learn, SHAP,
# Streamlit). Core workforce analytics and deterministic People Intelligence do
# not require those packages.

pandas==2.1.4
numpy==1.26.3
scikit-learn==1.5.2
lifelines>=0.27.0
scipy>=1.10.0
statsmodels>=0.14.0

# Visualization / exports used by local analytical capabilities
plotly==5.18.0
openpyxl==3.1.2
fpdf2==2.7.6

# Optional local synthesis client; Ollama server/model remains optional.
ollama==0.1.9

# Configuration / utilities
pyyaml==6.0.1
python-dateutil==2.8.2

# PyInstaller/setuptools runtime compatibility. Modern setuptools' vendored
# jaraco.context imports backports.tarfile at frozen-app startup; keep that
# namespace explicit so one-file builds boot consistently across OS runners.
backports.tarfile==1.2.0

# Local API
fastapi==0.133.0
# Explicit patched floor for request/form parsing and URL handling.
starlette==1.3.1
uvicorn[standard]==0.27.0
python-multipart==0.0.31
pydantic==2.9.2
